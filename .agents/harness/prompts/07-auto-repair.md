# 07 自动修复

根据 03、05、06 的失败证据执行最小修复。

1. 仅修改需求授权且与失败直接相关的路径。
2. 保留他人改动，不扩大需求范围，不降低校验标准。
3. 每轮修复后回跑 03、05、06，并记录轮次与结果。
4. 最多 3 轮；全部通过后进入 08。
5. 第 3 轮后仍失败，标记 `has_bugs` 并进入 08，不得声称完成。
6. 每轮输出变更、剩余问题与 `AGENT-HANDOFF`。
