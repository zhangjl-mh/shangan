# 04 前端调整

阶段顺序与完成条件以 `docs/harnesses/eight-stage/workflow.md` 为准，本文件只补充前端调整动作。

读取需求、已验证数据和页面局部规范。

1. 仅在授权前端路径内实现必要展示、交互或导出调整。
2. 复用现有组件和类型，避免无关重构。
3. 无前端工作时记录 `skipped: not_applicable`。
4. 不修改数据、Schema 或包文件。
5. 输出变更清单与 `AGENT-HANDOFF`，下一阶段为 05。
